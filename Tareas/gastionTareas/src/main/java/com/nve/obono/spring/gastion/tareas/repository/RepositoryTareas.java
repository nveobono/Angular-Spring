package com.nve.obono.spring.gastion.tareas.repository;

import com.nve.obono.spring.gastion.tareas.model.Tareas;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryTareas extends JpaRepository<Tareas, Long> {
}
